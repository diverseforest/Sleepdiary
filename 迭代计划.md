# 迭代 4：数据导入导出与文档 

 统一 exportPayload = {version, diaries:[…], questionnaires:[…]}；导出 JSON 和 CSV（CSV 至少包含日记核心字段与周报 KPI）。 
 导入流程分离校验（检查 version、缺字段）与写入；出现冲突时提示用户是否覆盖或合并。 
 在项目根新增 DATA_SCHEMA.md 描述字段、单位、示例，便于后续小程序/后端复用。 
 验收：已有导入导出按钮流程保持顺畅，错误提示到位。 

 # 迭代 5：小程序化准备 
 组件化：把表单处理、周报渲染、量表渲染拆分成可复用模块（即使仍存于 app.js，也以对象形式划分），确保逻辑层与 DOM 分离。 
 评估依赖：列出 Chart.js、localStorage 等在微信端的替代方案（Canvas、storage API），并在 README 注明兼容性与迁移步骤。 
 预留同步接口：定义 syncService 占位（当前 no-op），未来接入云存储或登录体系时只需实现接口。 
 验收：文档列出迁移 checklist（环境、API 映射、UI 适配）。 
 # 执行要求 
 逐迭代提交，可在每阶段结束前跑一次浏览器手测并记录已验证路径。 
 引入配置/数据文件时保持 ASCII，注释仅用于复杂逻辑（例如 scoring 公式）。 
 任何 JSON schema/常量在 README 或 DATA_SCHEMA.md 说明；导入导出逻辑需单独函数，方便单元测试。 
 若有图表或复杂 UI，先用静态数据验证，再接入真实数据，避免破坏现有历史列表。